name: MongoDB Backup

on:
  schedule:
    - cron: "17 1 * * *"
  workflow_dispatch:

permissions:
  contents: read

env:
  MONGO_URI: ${{ secrets.MONGO_URI }}

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - name: Install MongoDB Database Tools
        shell: bash
        run: |
          set -euo pipefail
          curl -fsSL https://www.mongodb.org/static/pgp/server-8.0.asc | sudo gpg --dearmor -o /usr/share/keyrings/mongodb-server-8.0.gpg
          echo "deb [ signed-by=/usr/share/keyrings/mongodb-server-8.0.gpg ] http://repo.mongodb.org/apt/ubuntu noble/mongodb-org/8.0 multiverse" | sudo tee /etc/apt/sources.list.d/mongodb-org-8.0.list
          sudo apt-get update
          sudo apt-get install -y mongodb-database-tools

      - name: Create backup
        shell: bash
        run: |
          set -euo pipefail
          test -n "$MONGO_URI"
          mkdir -p backup
          mongodump --uri "$MONGO_URI" --archive=backup/angels-home.archive.gz --gzip

      - name: Upload backup artifact
        uses: actions/upload-artifact@v4
        with:
          name: angels-home-mongodb-backup-${{ github.run_id }}
          path: backup/angels-home.archive.gz
          retention-days: 14
