const express=require("express");
const mongoose=require("mongoose");
const LibraryBook=require("../models/LibraryBook");
const {requireSchoolAuth,requireSchoolRole}=require("../middleware/schoolAuth");
const router=express.Router(); const validId=(id)=>mongoose.Types.ObjectId.isValid(id);
const admin= requireSchoolRole("admin");
router.get("/books",requireSchoolAuth,async(req,res,next)=>{try{const q=String(req.query.q||"").trim();const filter={isActive:true};if(q)filter.$text={$search:q};const books=await LibraryBook.find(filter).select("-loans").sort({title:1}).lean();return res.json({success:true,books});}catch(e){next(e);}});
router.get("/my-loans",requireSchoolAuth,async(req,res,next)=>{try{const books=await LibraryBook.find({"loans.borrower":req.schoolUser._id}).select("title author category loans").lean();const loans=[];books.forEach(b=>b.loans.filter(l=>l.borrower.toString()===req.schoolUser._id.toString()).forEach(l=>loans.push({...l,bookId:b._id,title:b.title,author:b.author})));return res.json({success:true,loans});}catch(e){next(e);}});
router.post("/books",requireSchoolAuth,admin,async(req,res,next)=>{try{const b=req.body||{};const copies=Math.max(1,Number(b.totalCopies)||1);const book=await LibraryBook.create({...b,totalCopies:copies,availableCopies:copies,isActive:true});return res.status(201).json({success:true,book:book.toObject()});}catch(e){next(e);}});
router.patch("/books/:id",requireSchoolAuth,admin,async(req,res,next)=>{try{if(!validId(req.params.id))return res.status(400).json({success:false,message:"Invalid book."});const book=await LibraryBook.findById(req.params.id);if(!book)return res.status(404).json({success:false,message:"Book not found."});const old=book.totalCopies;Object.assign(book,req.body);if(req.body.totalCopies!==undefined){const nextTotal=Math.max(1,Number(req.body.totalCopies)||1);const borrowed=old-book.availableCopies;book.totalCopies=nextTotal;book.availableCopies=Math.max(0,nextTotal-borrowed);}await book.save();return res.json({success:true,book:book.toObject()});}catch(e){next(e);}});
router.post("/books/:id/checkout",requireSchoolAuth,async(req,res,next)=>{try{if(!validId(req.params.id))return res.status(400).json({success:false,message:"Invalid book."});const book=await LibraryBook.findOne({_id:req.params.id,isActive:true});if(!book)return res.status(404).json({success:false,message:"Book not found."});if(book.availableCopies<1)return res.status(409).json({success:false,message:"No copies are currently available."});if(book.loans.some(l=>l.borrower.toString()===req.schoolUser._id.toString()&&l.status==="active"))return res.status(409).json({success:false,message:"You already have this book."});const days=Math.min(30,Math.max(1,Number(req.body?.days)||14));const due=new Date(Date.now()+days*86400000);book.loans.push({borrower:req.schoolUser._id,dueAt:due});book.availableCopies-=1;await book.save();return res.status(201).json({success:true,message:`Book issued. Due ${due.toISOString().slice(0,10)}.`,dueAt:due});}catch(e){next(e);}});
router.post("/books/:id/return",requireSchoolAuth,async(req,res,next)=>{try{if(!validId(req.params.id))return res.status(400).json({success:false,message:"Invalid book."});const book=await LibraryBook.findById(req.params.id);const loan=book?.loans.find(l=>l.borrower.toString()===req.schoolUser._id.toString()&&l.status==="active");if(!loan)return res.status(404).json({success:false,message:"Active loan not found."});loan.status="returned";loan.returnedAt=new Date();book.availableCopies=Math.min(book.totalCopies,book.availableCopies+1);await book.save();return res.json({success:true,message:"Book returned successfully."});}catch(e){next(e);}});
router.get("/admin/overview",requireSchoolAuth,admin,async(req,res,next)=>{try{const books=await LibraryBook.find({isActive:true}).lean();const allLoans=books.flatMap(b=>b.loans.map(l=>({...l,bookId:b._id,title:b.title})));const active=allLoans.filter(l=>l.status==="active");const overdue=active.filter(l=>new Date(l.dueAt)<new Date());return res.json({success:true,stats:{titles:books.length,totalCopies:books.reduce((n,b)=>n+b.totalCopies,0),availableCopies:books.reduce((n,b)=>n+b.availableCopies,0),activeLoans:active.length,overdueLoans:overdue.length}});}catch(e){next(e);}});
router.get("/admin/loans",requireSchoolAuth,admin,async(req,res,next)=>{try{const books=await LibraryBook.find({isActive:true}).populate("loans.borrower","name email role").lean();const loans=books.flatMap(b=>b.loans.map(l=>({...l,bookId:b._id,title:b.title,author:b.author})));return res.json({success:true,loans});}catch(e){next(e);}});
module.exports=router;
