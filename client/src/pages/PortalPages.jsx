import { useEffect, useState } from "react";
import PortalShell from "../components/PortalShell";
import { notificationApi } from "../services/api";
const rows = { pupils: [["AH-001", "Brian Mwangi", "Grade 6", "96%"], ["AH-002", "Faith Wanjiku", "Grade 5", "98%"], ["AH-003", "Daniel Otieno", "Grade 7", "91%"]], teachers: [["T-001", "Grace Njeri", "Mathematics", "Active"], ["T-002", "Peter Kamau", "English", "Active"], ["T-003", "Mary Achieng", "Science", "On leave"]], sponsors: [["SP-001", "Africa Education Trust", "12 pupils", "KES 840K"], ["SP-002", "Bright Futures", "8 pupils", "KES 520K"], ["SP-003", "Jane Mwende", "2 pupils", "KES 96K"]] };
export function Management({ type }) { const title = type[0].toUpperCase() + type.slice(1); return <PortalShell role="admin"><div className="mb-7 flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-widest text-amber-600">Administration</p><h2 className="text-3xl font-black text-blue-950">{title}</h2></div><button className="rounded-xl bg-blue-950 px-5 py-3 text-sm font-black text-white">+ Add {type.slice(0, -1)}</button></div><div className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wider text-slate-500"><tr>{["ID", "Name / Organisation", type === "pupils" ? "Class" : type === "teachers" ? "Subject" : "Supported", type === "pupils" ? "Attendance" : type === "teachers" ? "Status" : "Contributions"].map((h) => <th key={h} className="px-5 py-4">{h}</th>)}</tr></thead><tbody>{rows[type].map((r) => <tr key={r[0]} className="border-t border-slate-100"><td className="px-5 py-4 font-bold text-blue-950">{r[0]}</td>{r.slice(1).map((v) => <td key={v} className="px-5 py-4 text-slate-600">{v}</td>)}</tr>)}</tbody></table></div></PortalShell>; }
export function Notifications() { const [items, setItems] = useState([]); const [error, setError] = useState(""); useEffect(() => { notificationApi.list().then((r) => setItems(r.notifications || [])).catch((e) => setError(e.message)); }, []); const session = JSON.parse(localStorage.getItem("angelshome_session") || "null"); const role = session?.role || "pupil"; return <PortalShell role={role}><div className="mb-7"><p className="text-xs font-bold uppercase tracking-widest text-amber-600">Communications</p><h2 className="text-3xl font-black text-blue-950">Notifications</h2><p className="mt-2 text-slate-600">Your latest school announcements and important alerts.</p></div>{error && <p className="mb-4 rounded-xl bg-red-50 p-4 text-sm font-semibold text-red-700">{error}</p>}<div className="grid gap-4">{items.length ? items.map((item) => <article key={item._id} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex gap-4"><span className="flex h-10 w-10 shrink-0 items-center justify-center rounded-full bg-blue-50 text-blue-800">🔔</span><div><h3 className="font-black text-slate-900">{item.title}</h3><p className="mt-1 text-sm leading-6 text-slate-600">{item.message}</p><p className="mt-2 text-xs text-slate-400">{new Date(item.createdAt).toLocaleString()}</p></div></div></article>) : !error && <div className="rounded-2xl border border-dashed border-slate-300 bg-white p-12 text-center text-slate-500">No notifications yet.</div>}</div><div className="mt-7 rounded-2xl bg-emerald-50 p-6"><h3 className="font-black text-emerald-900">WhatsApp communication</h3><p className="mt-2 text-sm leading-6 text-emerald-800">For school-office communication, open WhatsApp or use the configured school WhatsApp Business integration.</p><a className="mt-4 inline-flex rounded-xl bg-emerald-600 px-5 py-3 text-sm font-black text-white" href="https://wa.me/?text=Hello%20Angels%20Home%20Education%20Centre" target="_blank" rel="noreferrer">Open WhatsApp</a></div></PortalShell>; }
export function Learning({ role }) { return <PortalShell role={role}><div className="mb-7"><p className="text-xs font-bold uppercase tracking-widest text-amber-600">Learning</p><h2 className="text-3xl font-black text-blue-950">My classes & learning</h2></div><div className="grid gap-5 md:grid-cols-3">{["Mathematics", "English", "Science", "Social Studies", "ICT", "Creative Arts"].map((s) => <div key={s} className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><p className="text-xs font-bold uppercase tracking-wider text-slate-400">Subject</p><h3 className="mt-2 text-lg font-black text-blue-950">{s}</h3><p className="mt-3 text-sm text-slate-600">Next lesson · 10:00 AM</p><div className="mt-4 h-2 rounded-full bg-slate-100"><div className="h-2 w-3/4 rounded-full bg-amber-400" /></div></div>)}</div></PortalShell>; }
