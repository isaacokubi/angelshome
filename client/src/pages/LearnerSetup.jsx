import{useState}from"react";import{useParams,useNavigate}from"react-router-dom";import{apiRequest}from"../services/api";
export default function LearnerSetup(){const{token}=useParams();const navigate=useNavigate();const[password,setPassword]=useState("");const[confirm,setConfirm]=useState("");const[email,setEmail]=useState("");const[message,setMessage]=useState("");const[error,setError]=useState("");const[saving,setSaving]=useState(false);
 const submit=async(e)=>{e.preventDefault();setError("");setMessage("");if(password.length<8)return setError("Password must be at least 8 characters.");if(password!==confirm)return setError("Passwords do not match.");setSaving(true);try{const r=await apiRequest("/admissions/setup/"+encodeURIComponent(token),{method:"POST",body:JSON.stringify({password})});setEmail(r.email||"");setMessage(r.message||"Password created successfully.");setPassword("");setConfirm("");}catch(e){setError(e.message||"This setup link is invalid or expired.");}finally{setSaving(false)}};
return <main className="min-h-screen bg-slate-100 px-4 py-12"><div className="mx-auto max-w-md"><div className="mb-6 text-center"><p className="text-xs font-black uppercase tracking-[0.2em] text-amber-600">Angels Home Education Centre</p><h1 className="mt-2 text-3xl font-black text-blue-950">Set up learner account</h1><p className="mt-2 text-sm text-slate-600">Create a secure password for the learner portal.</p></div><form onSubmit={submit} className="rounded-3xl border border-slate-200 bg-white p-6 shadow-lg">{error&&<div className="mb-4 rounded-xl bg-red-50 p-3 text-sm font-bold text-red-700">{error}</div>}{message&&<div className="mb-4 rounded-xl bg-emerald-50 p-3 text-sm font-bold text-emerald-800">{message}{email&&<div className="mt-2">Login email: <b>{email}</b></div>}<button type="button" onClick={()=>navigate("/login")} className="mt-3 rounded-lg bg-blue-950 px-4 py-2 text-xs font-black text-white">Go to login</button></div>}<label className="block text-sm font-bold text-slate-700">New password<input required minLength={8} type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-300 px-4 py-3" autoComplete="new-password"/></label><label className="mt-4 block text-sm font-bold text-slate-700">Confirm password<input required minLength={8} type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-300 px-4 py-3" autoComplete="new-password"/></label><button disabled={saving||Boolean(message)} className="mt-5 w-full rounded-xl bg-blue-950 px-5 py-3 font-black text-white disabled:opacity-50">{saving?"Creating password…":"Create learner password"}</button></form></div></main>;}
