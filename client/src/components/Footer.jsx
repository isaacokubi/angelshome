import { useSchoolSettings } from "../context/SchoolSettingsContext";

export default function Footer() {
  const { settings } = useSchoolSettings(); const { school, contact, footer, social, theme } = settings;
  const compact = theme.footerStyle === "compact"; const centered = theme.footerStyle === "centered";
  return <footer className="mt-12 text-white sm:mt-16" style={{ backgroundColor: "var(--school-primary)", borderRadius: centered ? "var(--school-radius) var(--school-radius) 0 0" : undefined }}><div className={`mx-auto grid max-w-7xl gap-8 px-5 py-10 sm:px-6 lg:px-8 ${centered ? "text-center lg:grid-cols-1" : "lg:grid-cols-[1.2fr_.8fr]"} ${compact ? "py-7" : ""}`}><div><h2 className="text-xl font-black">{school.name}</h2><p className="mt-3 max-w-xl text-sm leading-6 text-white/75">{footer.description || school.description}</p></div><div className={`text-sm text-white/75 ${centered ? "mx-auto" : ""}`}><p className="font-black text-white">Contact</p><p className="mt-2">{contact.phone}</p><p>{contact.email}</p><p className="mt-1">{contact.postalAddress || contact.address}</p><div className="mt-3 flex flex-wrap gap-3 ${centered ? "justify-center" : ""}">{[["Facebook", social.facebook], ["YouTube", social.youtube], ["Instagram", social.instagram], ["X", social.twitter]].filter(([, href]) => href).map(([label, href]) => <a key={label} href={href} target="_blank" rel="noreferrer" className="font-bold hover:text-[var(--school-secondary)]">{label}</a>)}</div></div></div><div className="border-t border-white/10 px-5 py-5 text-center text-xs text-white/60">© {new Date().getFullYear()} {school.name}. All Rights Reserved.</div></footer>;
}
